<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head.ejs') %>
    <link rel="stylesheet" href="\css\product-update-form.css" />
  </head>
  <body>
    <%- include('../partials/header.ejs') %>
    <main>
      <h1 class="title">Edición del producto: "<%=product.name%>"</h1>
      <div class="form-container">
        <form
          action="/products/edit/<%= product.id %> ?_method=PUT"
          method="POST"
        >
          <div class="field-wrap">
            <label for="name">Nombre de producto:</label>
            <input
              type="text"
              value="<%= product.name %>"
              name="name"
              id="name"
            />
          </div>

          <div class="field-wrap">
            <label for="price">Precio:</label>
            <input
              type="text"
              value="<%= product.price %>"
              name="price"
              id="price"
            />
          </div>

          <div class="field-wrap">
            <label for="description">Descripción:</label>
            <textarea name="description" class="" id="description">
            <%= product.description %></textarea
            >
          </div>

          <div class="field-wrap">
            <label for="code">Código:</label>
            <input name="code" class="" id="code" value="<%=product.code%> " />
            />
          </div>

          <div class="field-wrap">
            <label for="year">Año:</label>
            <input name="year" class="" id="year" value="<%=product.year%>" />
          </div>

          <div class="field-wrap">
            <label for="category">Categoría:</label>
            <input
              name="category"
              class=""
              id="category"
              value="<%=product.category%>"
            />
          </div>

          <div class="field-wrap">
            <label for="size">Talla:</label>
            <input name="size" class="" id="size" value="<%=product.size%>" />
          </div>

          <div class="field-wrap">
            <label for="image">Imagen:</label><br />
            <img class="image" src="\images\<%=product.image%>" /><br />
            <input type="file" name="image" id="image" />
          </div>

          <div class="field-wrap">
            <button type="submit">Actualizar</button>
          </div>
        </form>

        <form
          action="/products/delete/<%= product.id %>?_method=DELETE"
          method="POST"
        >
          <button type="submit">Borrar</button>
        </form>
      </div>
    </main>
    <%- include('../partials/footer.ejs') %>
  </body>
</html>
